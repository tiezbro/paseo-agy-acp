TASK_ID: S3-T05
EVIDENCE: green-1 timeout deletes payload
UTC: 2026-08-16T01:24:36Z
HEAD: 83fb97257814f9072770c77d1bb0da4cfa2b420e

Contract assertion:
- enqueue now=0 with queue_timeout_ms=1800000.
- admitNext now=1860000 returns no lease.
- request state is queue_timeout.
- turn_payloads row count for the request is 0.

Command:
rtk npm test -- tests/admission-timeout-contract.test.ts

Exit code: 0

Key output:
> npm run typecheck && npm run build && vitest run tests/admission-timeout-contract.test.ts
> tsc -p tsconfig.test.json
> node scripts/clean-dist.mjs && tsc
Test Files  1 passed (1)
Tests  2 passed (2)

Impact before write:
- expireQueued upstream impact: LOW; direct callers admitNext, admitRequest, getQueueSnapshot; affected processes admit and waitForLease; affected modules Admission Controller direct and Admission indirect.
- selectEligibleRequest upstream impact: LOW; direct callers admitNext and admitRequest; affected processes admit and waitForLease; affected modules Admission Controller direct and Admission indirect.

Method evidence:
- tdd SKILL.md read fully; green regression task, RED receipt contract inapplicable by S3-T05 authority.
- implement SKILL.md read fully; implementation executed only for test/receipt write set. Commit/code-review instructions are inapplicable because this leaf task forbids delegation, staging, commit, and source changes.
- S2-SPEC-001 C2 and S2-TEST-001 M3 were read before writing.
- No production source, crypto, schema, git index, refs, provider, install, switch, or production daemon writes.
