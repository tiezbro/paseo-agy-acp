# paseo-agy-acp Stage 2 ACP Connector Source-Reconnaissance Map

状态：Stage 2 source reconnaissance artifact；canonical task identity `S2-RECON-ACP-001`；repair rounds REPAIR-001/002/003/004/005/006 的历史记录保留在本文件内部（§0.1、§13），不作为本文件的 task identity；只绑定当前 live source、deterministic test source 与 integration seam 的静态证据；不表示当前实现已验收。
TASK_ID：S2-RECON-ACP-001
STAGE：2
TASK_TYPE：source reconnaissance（ACP Connector 功能区）
业务边界：`ACP Connector/` 全部业务义务 + `Admission Controller/` 的跨区 integration seam（只读证据）；与 Admission source map（`docs/design/v2.0.0.0-stage2-admission-source-map.md`，task S2-RECON-ADM-001R）逐行对齐，消除矛盾；不重写 Scheme、不新增源码。

## 0. Authority、基线与方法证据（读前准入）

| 项 | 值 | 核验 |
| --- | --- | --- |
| Scheme path | `/home/tiezbro/projects/MAACS/docs/maacs-paseo-agy-acp-confirmed-scheme.md` | live SHA-256 `d3b712ee5dd01029c66b0372a482f248a6f1ecbcf81572eb74d7b4ba5d98a98d` ✓ 状态 `confirmed` ✓ |
| Controller Prompt | `/home/tiezbro/projects/MAACS/docs/controller-prompts/PASEO_AGY_ACP_STAGE2_CONTROLLER_PROMPT.md` | SHA-256 `9f946e36e717547e5982eefcc37c188d236e48b3be495541bd12129f84207993` ✓ |
| AGENTS.md | `AGENTS.md` | SHA-256 `9bbd1e7b1bb7938cc3fd5922a1dcf9a036fa8639267d38789213f4fe9fc3198d` ✓ |
| Git baseline | `main` HEAD `2229353676ada1531c95763eca4a3a25fccdbed3` | `git rev-parse HEAD` 一致 ✓；worktree 仅含本 worker 与其他已授权并行 worker 的 untracked artifacts ✓ |
| 503 锁定处置 | `docs/design/v2.0.0.0-stage2-503-feasibility.md`（task S2-SPIKE-503-001；本文件不携带其 digest，handoff 以 commit/blob/SHA 外部绑定） | 锁定处置为完整三元组 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`，typed stop（§6 采用）；可信 capacity source 外部未解决，无 trusted-503 RED/实现就绪，不授权任何 phrase/regex 归一化 | 
| Admission source map | `docs/design/v2.0.0.0-stage2-admission-source-map.md`（task S2-RECON-ADM-001R；本文件不携带其 digest，handoff 以 commit/blob/SHA 外部绑定） | 本文件逐行对齐其 ADM-R01/R03/R04/R06/O01/D03/D04 行（对齐内容见 §2/§13），不引用其 digest |

无 `SCHEME_HASH_MISMATCH`、无 `DIRTY_OWNERSHIP_UNKNOWN`、无 `AUTHORITY_CONFLICT`（仓库内未发现第二份声称有效的 confirmed Scheme；`docs/design/v2.0.0.0-admission-controller.md` 自封「已确认」按 F8 处置，见 §11）。

### 0.1 本 repair round 修正摘要（repair history；canonical identity 为 S2-RECON-ACP-001）

1. 删除旧版 §2.6/§2.8 中的假阳性：startup recovery **不**证明 queued owner、pre-identity、全 phase、无重启的 bounded runtime recovery、或「三次 recovery_required 不烧满 3 席」；production `runPromptCommand` 已有 inline dispatch hooks，但**不**消费 prompt-free primitive 的 accepted/ambiguous 三态结果（`cli.ts:1132-1142`）。
2. 新增显式 permission-chain finding：admission enabled 强制 `dangerously-skip-permissions`（`setup.ts:44-49`、`agent.ts:480-488`、`cli.ts:535-541`），Scheme §4.2:98 要求保留权限链 → 判定为偏离 Scheme，Stage 3 修复；`session/request_permission` 所有权保留；区分 PTY 权限按键写与首次不可逆业务 prompt 写。
3. 503 单一 disposition：classifier 与 cooldown ledger 是正确回归证据（保留），trusted source 是完整三元组 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`（采纳 feasibility doc 锁定处置）；可信 capacity source 外部未解决、无 trusted-503 RED/实现就绪、不授权任何 phrase/regex 归一化；429/auth/permission/timeout/transport 保持独立。
4. v2 failure → `end_turn` 是 source-proven 偏离；v1 typed failure contract 缺失；不声称「cancel 双终态」。
5. 每行一个 disposition：拆分旧版混合标记行，与 Admission map ADM-R01/R03/R04/R06/O01/D03/D04 对齐。
6. F7 canonical 目标固定为 `agent_id/agentId`；当前 `parent_id/parentId` 拼写仅是 source-gap migration input。
7. 本文件不内嵌自身 hash；whole-file SHA-256 在文件外计算并报告。
8. REPAIR-004 锁定 prompt-turn terminal 的唯一物理 owner 为 `acp/session/prompt.ts`，并固定 v2 单 idle `state_update` 与 v1 单 `PromptResponse` 的精确 wire tuple；辅助 terminal display 与 session-update converter 不取得 owner 权限。
9. REPAIR-005 将每个 trusted-503 引用统一为完整 typed-stop 三元组，并保持 source-gap 行只有一个 disposition；classifier/cooldown regression 与上游 source 缺口继续分行绑定。
10. REPAIR-006 采纳 fresh Formal Spec Review LOW-1，将 production dispatch 证据精确化为“既有 `TurnDispatchBoundary` hooks 已接线、prompt-free accepted/ambiguous 三态结果未接线”；不改变 ADM-R03 disposition 或 T12 target。

## 1. Method Conformance Evidence（Skill 与控制方法，exact hashes）

| Skill / 方法 | installed path | SHA-256 | 逐项执行 |
| --- | --- | --- | --- |
| scheme-bound-artifact-repair（primary，REPAIR-002） | `/home/tiezbro/.agents/skills/paseo/scheme-bound-artifact-repair/SKILL.md` | `3e3293f1a346342a2e66bd4049a796a7947cc7e0f6fcb161ee727a127f9bfd5f` | 完整读取；hash-first、逐 patch 后重跑 literal acceptance scans、最后外部计算 whole-file SHA-256，全部执行 |
| stage2-source-reconnaissance（supporting，控制方法） | `/home/tiezbro/.agents/skills/paseo/stage2-source-reconnaissance/SKILL.md` | `b1b6361d66834377a5b4208d8b88a69890068b35147aec9f2f81b96a3319539e` | 完整读取；source/test 证据在本文件 §2-§10 保持 live 绑定（REPAIR-002 未改动任何 source 结论行） |
| scheme-artifact-authoring（supporting，控制方法） | `/home/tiezbro/.agents/skills/paseo/scheme-artifact-authoring/SKILL.md` | `eb0376529ee6d1e5d201b5ca085838515e37ecd9070d0f08b8ee618307091254` | 完整读取；内部 hash 禁令、外部 whole-file SHA-256、禁用选择式措辞均执行 |
| research（primary，REPAIR-001 起） | `/home/tiezbro/.agents/skills/research/SKILL.md` | `af378829f015775a3bcd65ff466826722e99359017ae6bae227ca4c9bd14049c` | 见下 |
| wayfinder（supporting） | `/home/tiezbro/.agents/skills/wayfinder/SKILL.md` | `d33e2141f7c8bbfd137fef0213cbec465820e4680e67da5d0f0815d6742d26c2` | 见下 |
| triage（supporting） | `/home/tiezbro/.agents/skills/triage/SKILL.md` | `91e2817ecb688c4df4e2444eab472d1d79d2a0a57abf9f6726967664c460ff2e` | 见下 |

- **research**：逐 claim 调查 primary sources（Scheme → live source → deterministic test）并绑定 file:line：**executed**（本文件全部矩阵）；「Spin up a background agent」：**inapplicable**（bounded child，PASEO bootstrap 禁止委派）；「写入 repo 内单个 Markdown 文件」：**executed**（本文件即唯一写入目标）。
- **wayfinder**：issue tracker 地图/decision tickets：**inapplicable under project authority**（外部 tracker 不在授权内）；「义务→唯一 disposition」的本地决策地图：**executed（适配）**。
- **triage**：issue tracker 状态机：**inapplicable under project authority**；verified-source-fact vs source-gap vs false-positive-test 分离：**executed（适配）**；`.out-of-scope/` KB：**inapplicable**（仓库无该目录）。
- FORBIDDEN_SHORTCUTS 遵从：未信任名字/文档/测试本身（每条结论回到 live source 行）；未臆造 stderr/provider 输出（503 分支按 feasibility doc 停止）；未在单行写复合 disposition；未改 source/test/其他文档；未 build/test/provider/6767/network/commit；未委派。

## 2. 义务 → Source symbol → Deterministic test → Integration/runtime evidence → Disposition 矩阵

标记约定：✅=已正确实现且保留；🔧=与 Scheme 偏离/缺口，Stage 3 修复；🧪=测试不足或假阳性；❓=尚未真实验证；⛔=已删除且禁止恢复。**每行有且只有一个 disposition。** `file:line` 均为 live HEAD 证据。与 Admission map 重合的行标注其 ADM 行 ID。

### 2.1 席位（Scheme §4.1）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| 默认共享池最大活动 turn = 3；显式 3 正常（ADM-S01） | `Admission Controller/runtime-config.ts:27-33`；`:60-65` | `tests/admission-runtime-config.test.ts:92-101` | `README.md:92`、design doc:43 一致；`controller.ts:1217-1219` | ✅ |
| 唯一整体降级 1（ADM-S02） | `runtime-config.ts:64`；`controller.ts:1613-1615` | `tests/admission-runtime-config.test.ts:70-90,103-121`；`tests/admission-controller.test.ts:76-87` | `README.md:99-100` | ✅ |
| `0/2/4/5/负数/非整数/其他值` fail closed（源码行为，ADM-S02） | `runtime-config.ts:137-143` 正则 + `:142-144`；`controller.ts:1607-1617` | `tests/admission-runtime-config.test.ts:103-121`；`tests/admission-controller.test.ts:76-87` | — | ✅ |
| 三份文档对非法值枚举弱于 Scheme（F17，文档层） | `README.md:100`、`README.zh-CN.md:96`、`CHANGELOG.md:17`、design doc:50-51 只写「2,4,5 及其他」 | — | Scheme §4.1:85 枚举含 0/负数/非整数 | 🔧 Stage 3 文档集成统一枚举（不属本 worker 写集） |
| `1` 是共享池单席，非每 Connector 一席（ADM-S01/S03） | `controller.ts:1207-1218` `activeLeaseCount` 跨全部 lease | `tests/admission-controller.test.ts:62-74` | 跨进程 `tests/admission-process.test.ts:27-43` | ✅ |
| 同一 OS 用户 + 同一 `AGY_ACP_STATE_DIR` 共享同一池（ADM-S03） | `runtime-config.ts:57`；`tests/helpers/admission-controller-child.mjs:13-24` | `tests/admission-process.test.ts`；`tests/admission-key-store.test.ts:213-227` | `README.md:98` | ✅ |
| 多 Connector 同一共享库 policy 必须一致，不一致拒绝启动（ADM-S04） | **无实现**：`controller.ts:1607-1617` 只做单值校验；schema 无 policy 记录（`schema.ts:105-255`）；`assertNoUnexpectedTables`（schema.ts:281-291） | **无测试** | 无文档 | 🔧 Stage 3 需 policy 一致性记录或启动期跨进程比对 fail closed（F2，blocking） |
| 3→1 软排空（ADM-S05） | **无运行时机制**：policy 构造期冻结（`runtime-config.ts:50-99`；`controller.ts:379-391`） | **无测试** | design doc:43-51 无过程描述 | 🔧 Stage 3 需运行时 policy 切换 + 排空状态机（F1，blocking） |

### 2.2 一条消息的完整流程（Scheme §4.2）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| 用户消息先取本地 turn 顺序权，再进共享队列（ADM-Q01） | `acp/session/turn-scheduler.ts:139-265`；`acp/session/prompt.ts:646-832`；`prompt.ts:401-408` | `tests/queue-steer.test.ts:679-835,344-385` | `turn-coordinator.ts:123-141` | ✅ |
| 队列记录请求、agent key、model、入队时间；prompt 加密保存（ADM-Q01） | `controller.ts:1385-1426,1428-1452,1367-1383`；`turn-coordinator.ts:133-141` | `tests/admission-turn-coordinator.test.ts:148-186`；`tests/admission-controller.test.ts:35-45` | schema `turn_payloads`（schema.ts:164-178） | ✅ |
| Controller 选最老且符合条件请求；只有拥有该请求的 Connector 可领取（ADM-Q02） | `controller.ts:1259-1286,517-528`；`turn-coordinator.ts:194-228` | `tests/admission-controller.test.ts:89-101` | — | ✅ |
| lease 后等全局启动闸门；同时最多一个 start；间隔 ≥ 2 秒（ADM-Q04） | `controller.ts:552-572,1221-1238` | `tests/admission-turn-coordinator.test.ts:83-146`（start_history 精确断言） | `turn-coordinator.ts:164` | ✅ |
| 启动后继续走原 `AgyCliSession.prompt` + conversation SQLite + StreamPoller + Translator + 在线 ACP 通知链（ADM-O02） | `turn-coordinator.ts:146-181`；`prompt.ts:450-476,524-555`；`agy/cli.ts:521-568`；`agy/db/streaming.ts`；`agy/db/translator.ts`；`acp/session/update-wire.ts` | `tests/cli.test.ts`、`tests/db.test.ts`、`tests/acp-server.test.ts:341-432` | `acp/agent.ts:645-719` 注入 `turnAdmission`+`sessionStore` 不替换原链 | ✅ |
| 启动后权限链保留（Scheme §4.2:98「权限与在线 ACP 通知链」） | **偏离**：`setup.ts:44-49` 与 `agent.ts:480-488` 在 admission enabled 时强制 `dangerously-skip-permissions`；`cli.ts:535-539` 拒绝 interactive permission 模式；权限链被旁路 | 无 admission×permission 链测试 | `session/request_permission` 所有权仍在 `acp/session/request-permission.ts`（v1/v2），但 enabled 时不触发 | 🔧 **偏离 Scheme，Stage 3 修复（PERMISSION_CHAIN_AMBIGUOUS 已判定为偏离，见 §4.4）** |
| 运行期更新 lease heartbeat；过期只标可疑，不直接回收（ADM-R01） | `controller.ts:751-756` 只写 `heartbeat_at`；`turn-coordinator.ts:153-161` interval + `heartbeatFailed` 标记 | `tests/admission-turn-coordinator.test.ts:219-259`（只证写入推进与成功释放） | **无 TTL、无 expired/suspect 状态、无 reaper、无 runtime expiry 证据**；`heartbeatFailed` 仅在 execute 抛错路径触发 | 🔧 Stage 3 需 heartbeat expiry/suspect 语义（与 ADM-R01 一致） |
| Provider 明确终态后同一 Controller 事务记录终态并立即释放席位（ADM-Q05） | `controller.ts:712-749` `completeLiveTurn` 单事务 | `tests/admission-turn-coordinator.test.ts:261-296,219-259` | — | ✅ |
| 同 Agent/session 下一条消息重新排队，不继承席位 | `turn-coordinator.ts:123-141`；`prompt.ts` 每次独立 claim | `tests/admission-turn-coordinator.test.ts:83-146` | — | ✅ |
| 空闲 session/连接零席位（seat 计数维度，ADM-S01） | `controller.ts:1207-1215` 只数 lease；`sessions` 表不参与 | `tests/admission-controller.test.ts:62-74` | — | ✅ |
| 空闲 session/连接零 resident turn process（真实运行证据维度，ADM-D03） | `agy/cli.ts` PTY 仅 prompt 时创建（`runInteractivePrompt` 内 `freshPty`）；`acp/agent.ts:174-214` 构造不 spawn | 未发现专门验证 idle connection zero resident process 的 admission integration test | real process residency/close/delete cross-layer evidence 未真实取得 | ❓ 尚未真实验证（与 ADM-D03 一致；Stage 3 test worker 需 real evidence） |
| 关闭/删除 session：queued 取消删 payload；running 走现有取消终态确认（ADM-D03 后半） | `acp/session/close.ts:11-26`、`delete.ts:21-37`；`cancel.ts:26-37`；admission 侧 `controller.ts:484-498` `cancelQueued` | `tests/queue-steer.test.ts:2287-2351,1063-1123`；`tests/acp-server.test.ts:1448-1496` | source 路径存在：`turns.close()`→`cancelQueuedPrompts`→`agy.close()`；admission 侧 `cancelQueued` 同事务删 payload；真实 cross-layer close/delete runtime evidence 的缺口并入上一条 ❓（ADM-D03 前半） | ✅（source 路径） |

### 2.3 排队顺序与公平（Scheme §4.3）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| oldest-eligible + per-agent fairness；fairness key = 可信注入 `PASEO_AGENT_ID`（ADM-F01） | 值来源正确：`acp/agent.ts:662` `parentId: runtimeConfig.agentId`（=`runtime-config.ts:58` `requirePaseoAgentId`）；`controller.ts:1272-1286` 按 `parent_id` 列选择 | `tests/admission-controller.test.ts:89-101`；`tests/admission-runtime-config.test.ts:61-68` | — | 🔧 **F7（命名 authority 冲突）**：canonical 目标固定为 `agent_id/agentId`；Stage 3 SHALL 以 `agent_id/agentId` 为 fairness-key 名称（含兼容 migration）；当前 `parent_id/parentId` 拼写仅是 source-gap migration input，不得作为保留命名 |
| 无活动 turn 的 agent 优先；同层先入队先启动（ADM-Q03） | `controller.ts:1274-1285` | `tests/admission-controller.test.ts:89-101` | design doc:80-81 | ✅ |
| 每 session 单 turn 顺序（ADM-F02） | `turn-scheduler.ts`；`prompt.ts:655-661` | `tests/queue-steer.test.ts:618-678,1732-1796` | — | ✅ |
| cooldown/cancel/timeout 不堵后续 eligible（ADM-Q03） | `controller.ts:1283,1188-1205`；`cancelQueued` | `tests/admission-controller.test.ts:62-74`；`tests/admission-turn-coordinator.test.ts:148-186` | — | ✅ |
| queued owner 崩溃、pre-identity 崩溃、owner 消失有有界处置（ADM-R04/R06） | `startup-recovery.ts:46-106` 仅启动期、仅处理已有 lease；`controller.ts:805-854`；`:182-190` processIdentity nullable | `tests/admission-startup-recovery.test.ts:155-199` 只证已有 dispatch 的启动恢复 | queued 无 owner/process identity；admitted/starting null identity 直接 retained；**无运行期 reaper** | 🔧 Stage 3 需逐 phase bounded recovery（与 ADM-R04/R06 一致） |
| 排队最多 30 分钟；超时记录 `queue_timeout` 并同事务删 payload（ADM-Q06） | `controller.ts:1188-1205`；`turn-coordinator.ts:208` | `tests/admission-turn-coordinator.test.ts:188-217` | `turn-coordinator.ts:62-67` | ✅ |
| 排队进度只读观察，不改顺序（ADM-Q07） | `controller.ts:530-550`；`turn-coordinator.ts:267-286`；`prompt.ts:411-420` | `tests/admission-turn-coordinator.test.ts:148-186` | — | ✅ |

### 2.4 账号边界与权限（Scheme §4.4）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| 当前 AGY 无可信账号 ID，不伪造账号指纹 | `schema.ts:123` `fingerprint` = `requestId` 镜像（`turn-coordinator.ts:137`），非账号指纹；`controller.ts:1454-1460` `contentFingerprint` 为 payload 内容 HMAC | `tests/admission-schema.test.ts` | 两种 fingerprint 语义文档零区分（F12 内嵌，Admission worker 主责） | ✅（行为） |
| 每账号独立 `AGY_ACP_STATE_DIR`（ADM-A01 前半） | `runtime-config.ts:57,119-127`；`key-store.ts:37-49`；`admission/runtime.ts:55-68` | `tests/admission-runtime-config.test.ts:54-59` | `README.md:86-88` | ✅ |
| Admission 启用时 ACP 在线 login/logout 必须 fail closed（ADM-A01 后半） | **无实现**：`acp/auth/login.ts:8-15`、`acp/auth/logout.ts:8-15` 直接委托；`acp/authenticate.ts:10-24`、`acp/logout.ts:9-27` 无门禁；`Admission Controller/` 全目录 grep `login\|logout` 0 命中 | **无测试**（`tests/acp-server.test.ts:123-190` 仅无 admission 场景） | 仅 design doc:53-54 一句 | 🔧 **MISSING（F3，blocking）**：Stage 3 SHALL 在集成 seam 实现 login/logout 门禁（enabled→typed reject；disabled→原行为）+ 测试矩阵项 5 |
| 换号前排空 queued/active、停 Connector、独立 state dir 重启 | 文档层 design doc:54-55；无运行时账号切换机制 | — | — | ✅（操作流程保留） |
| Admission 关闭时保留原 Connector 登录行为（ADM-B02） | `runtime-config.ts:53-55`；`acp/agent.ts:706-718`；`acp/agent.ts:278-293` 原路径 | `tests/admission-runtime-config.test.ts:20-28` | `README.md:86` | ✅ |

### 2.5 503、失败与 cooldown（Scheme §4.5）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| 结构化分类器与 cooldown ledger（ADM-E01） | `ACP Connector/admission/errors.ts:36-56`；`controller.ts:726-737`；`:758-771` | `tests/admission-errors.test.ts:75-108`（结构化输入全矩阵）；`tests/admission-turn-coordinator.test.ts:261-296`（cooldown 写入） | 分类器只证明「给定结构化输入如何分类」；ledger 只证明「分类结果如何持久化」 | ✅（classifier/ledger 回归证据，与 ADM-E01 一致） |
| 可信 503 必须绑定真实结构化字段或真实 CLI 输出合同（ADM-E02，F11） | `turn-coordinator.ts:366-385` `failureFromAgyError` 用 `error.message+error.stderr` 正则猜 503/429/401/403；`AgyCliError`（cli.ts:295-312）无结构化 provider 字段；`stream-json-identity.ts:217-244` 丢弃 result status/error | `tests/admission-turn-coordinator.test.ts:267-272` 臆造 stderr（fake oracle，非 provenance） | **SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN**（完整三元组，采纳 feasibility doc 锁定处置）：静态二进制 phrases（UNAVAILABLE/MODEL_CAPACITY_EXHAUSTED 等）可对应多个错误类（如 MODEL_CAPACITY_EXHAUSTED 与 RATE_LIMIT_EXCEEDED），不能证明 carrier/emission；官方文档只有通用 status/error；本地二进制版本/hash 已知但无绑定 source commit 或可复现 call path；可信 capacity source 外部未解决，无 trusted-503 RED/实现就绪，不授权任何 phrase/regex 归一化；Stage 3 trusted-503 依赖门关闭（feasibility doc §6） | **SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN**（完整三元组，typed stop，分支停止） |
| 503 provider/model 暂停 30 秒；cooldown 只改 eligible 不占席（ADM-E01） | `runtime-config.ts:32`；`controller.ts:726-737`；`:1283,1288-1297` | `tests/admission-turn-coordinator.test.ts:261-296`（`not_before:31_000`） | — | ✅ |
| 429 quota、auth、permission、timeout、transport 独立分类（ADM-E02 后半） | `errors.ts:36-56` 七类；`controller.ts:101-110,2017-2047` | `tests/admission-errors.test.ts:44-51,85-108` | 分类器维度独立成立；上游信号来源与 503 共用 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN` 外部证据门（见上行，不重复计 disposition） | ✅ |
| Connector 不做外层业务 prompt 自动重试（ADM-E03） | `turn-coordinator.ts` 无重试路径；`dispatch-boundary.ts:129-182` 单次；`prompt-free-process.ts:110-127` once-only | `tests/admission-fault-security-matrix.test.ts:121-151`；`tests/agy-dispatch-boundary.test.ts:169-191` | — | ✅ |
| ACP v1/v2 把 queue timeout、取消、Provider 失败、`recovery_required` 呈现为真实 typed terminal，不伪装成功 `end_turn`（ADM-O01） | v1：`prompt.ts:709-712` failure 抛原 error（无 typed failure 响应合同）；v2：`prompt.ts:877-886,1017-1023` failure handler **一律 `emitTerminal("end_turn")`**——source-proven 偏离 | 无 typed-terminal 测试（`tests/admission-turn-coordinator.test.ts:210,317` 只到 Coordinator 层） | `AdmissionTurnExecutionResult.stopReason` 仅 2 值（turn-coordinator.ts:35-37）；`v2TerminalEmitter`（prompt.ts:567-593）仅 `end_turn\|cancelled` | 🔧 **MISSING（F4，blocking）**：v2 failure→end_turn 是 source-proven 偏离；v1 typed failure contract 缺失；Stage 3 SHALL 定义 v1/v2 typed terminal 合同（见 §5.2） |

### 2.6 heartbeat、取消与崩溃恢复（Scheme §4.6）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| heartbeat 过期只标可疑，不直接释放（ADM-R01） | 见 §2.2 行；`controller.ts:751-756` 只写、无 expiry 语义 | `tests/admission-turn-coordinator.test.ts:219-259` | 无 TTL/suspect/reaper | 🔧（与 ADM-R01 一致，不重复计 disposition） |
| 槽位回收核验 connector/child/pgrp/descendant/PID reuse；单看 PID 不足（ADM-R02） | `process-evidence.ts:214-257`；`startup-recovery.ts:108-133` `inspectProcessGroup`；`owner-instance.ts:40-77` | `tests/admission-process-evidence.test.ts:184-200`；`tests/admission-startup-recovery.test.ts:173-199` | schema `lease_process_identities` 22 列（schema.ts:180-211） | ✅（本地证据 primitive，与 ADM-R02 一致） |
| queued/admitted/starting/active/recovery_required owner 崩溃都有运行期或启动期有界恢复（ADM-R04） | `startup-recovery.ts:46-106` 仅启动期、仅已有 lease；无运行期 reaper | `tests/admission-startup-recovery.test.ts` 只证启动期 | null identity 直接 retained；queued 无 owner | 🔧（与 ADM-R04 一致） |
| queued owner、pre-identity 崩溃有界，不堵 30 分钟/不丢席（ADM-R06） | `controller.ts:182-190` nullable；`startup-recovery.ts:64` | 未发现 queued owner/pre-identity bounded test | PASEO_AGENT_ID 不是 queued owner lease | 🔧（与 ADM-R06 一致） |
| 明确证明 prompt 未发送可结束请求释放席位；本版本不自动重放（ADM-R05） | `controller.ts:620-641` `abandonBeforePrompt`；`turn-coordinator.ts:166-172` | `tests/admission-fault-security-matrix.test.ts:99-119` | — | ✅ |
| prompt 可能已发送/状态不明→删可重放 payload+`recovery_required`，零自动重放（ADM-R05） | `controller.ts:644-670`；`turn-coordinator.ts:245-256` | `tests/admission-turn-coordinator.test.ts:298-325` | `README.md:120-123` | ✅ |
| 明确证明本地进程树已退出→释放本地席位，保留 recovery_required（ADM-R05） | `controller.ts:677-706`；`startup-recovery.ts:76-87` | `tests/admission-startup-recovery.test.ts:155-171` | journal `request_recovery_seat_released` | ✅（已证明退出路径） |
| cancel/steer/disconnect/heartbeat failure/terminal race 无重复 dispatch、无重复终态、无永久占席（ADM-R07） | source 层：`turn-scheduler.ts` 幂等 release；`controller.ts:1299-1315,1472-1489` lease fence；`settleFailure`（turn-coordinator.ts:230-257）先查终态 | `tests/queue-steer.test.ts` 35 用例（单进程）；`tests/admission-fault-security-matrix.test.ts` | 缺少跨进程 terminal/heartbeat/disconnect race 全覆盖；「cancel 双终态」未被任何测试或 source 证明存在 | 🧪 **测试不足或假阳性**（与 ADM-R07 一致）：source 层单终态机制存在，但跨进程 race 全矩阵未证明；不声称双终态缺陷也不声称全无 race |
| 完成/失败/取消后立即释放席位（ADM-Q05） | `completeLiveTurn`/`abandonBeforePrompt`/`releaseExitedRecoverySeat` 同事务删 lease | `tests/admission-turn-coordinator.test.ts:219-259,261-296` | — | ✅ |
| 三次不确定事件不永久烧满 3 席且不靠重启恢复 | `activeLeaseCount`（controller.ts:1207-1215）含 recovery_required；`releaseExitedRecoverySeat` 仅由 `startup-recovery.ts:80` 调用（启动期） | 未发现「3×recovery_required + 无重启」用例 | **无运行期释放路径**：三次不确定在无重启时确实烧满 3 席 | 🔧 Stage 3 需运行期 bounded 释放路径或显式 reaper（与 ADM-R04/R06 一致；旧版 ✅ 判定为假阳性已删除） |

### 2.7 数据与输出边界（Scheme §4.7）

| Scheme 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| 单一 durable state authority（ADM-D01） | `controller.ts:385-391`；`schema.ts`；`key-store.ts`；`key-derivation.ts` | `tests/admission-schema.test.ts`；`tests/admission-key-store.test.ts`；`tests/sqlite-session-store.test.ts:253-337` | WAL/FULL/busy_timeout | ✅ |
| 逐表/字段/索引/约束业务义务映射 + policy 一致性记录 + identity 保存（ADM-D04，F12） | schema.ts:105-255 完整定义；**无 policy 记录**；fingerprint 双语义未文档化 | `tests/admission-schema.test.ts` | — | 🔧（与 ADM-D04 一致；本文件 §2.1/§2.6 提供 seam 侧输入） |
| Admission 默认关闭；关闭时 legacy 行为与测试不受影响（ADM-B02） | `runtime-config.ts:53-55`；`acp/agent.ts:706-718` | `tests/admission-runtime-config.test.ts:20-28`；`tests/acp-runtime-wiring.test.ts:59-72` | — | ✅ |
| 启用后复用唯一 ACP 在线输出路径，不增加第二 live 路径/outbox/ACK/replay/fencing/parity/custom identity/requeue（ADM-O02 + forbidden） | `prompt.ts:450-555` 唯一 forwardUpdates；`schema.ts:281-291` 意外表 fail closed；CHANGELOG.md:36-46 | `tests/admission-fault-security-matrix.test.ts` | 无 outbox/ACK/replay 表或 API | ✅（absence 即满足） |
| 未使用的 reserved 派生 key 用途 | `key-derivation.ts:13-21` 有 `delivery-identity`/`claim-token` 用途名，**无表、无使用点**（grep 仅 derivation 自身） | 无 | — | 🧪 未使用残留；Stage 3 清理或记录为 reserved，不得据此恢复 delivery surface |
| 官方 session/load、session/resume、conversation history 归 ACP Connector；断连不重放（ADM-O02） | `acp/session/load.ts`、`resume.ts`、`setup.ts:93-107`；`agy/db/replay.ts`；`acp/agent.ts:496-513` | `tests/acp-server.test.ts:434-595,1611-1721` | `README.md:115-116` | ✅ |

### 2.8 进程与启动边界（native Pipe+EOF、SIGTERM/SIGKILL、descendant cleanup）

| Scheme/Controller 义务 | Source symbol | Deterministic test | Integration/runtime evidence | Disposition |
| --- | --- | --- | --- | --- |
| prompt-free primitive：stdin 单写 + EOF，accepted/ambiguous 不可重试（ADM-R03 前半） | `agy/prompt-free-process.ts:110-127` `writeBusinessPrompt` once-only；`cli.ts:1132-1142` production inline-hook path 未调用该 primitive | `tests/prompt-free-process.test.ts:27-83,76-79,273-314` | primitive 本身被 fake-child 测试证明 | ✅（primitive 正确，与 ADM-R03 拆分） |
| production `runPromptCommand` 必须消费 primitive 的 accepted/ambiguous 写结果（ADM-R03 后半） | `cli.ts:1132-1142`：`boundary.prepare/beforePromptWrite` 后执行单次 stdin 写并调用 `afterPromptWrite`；现有 hooks 已接线，但未调用 `writeBusinessPrompt`、未取得三态结果、未映射 ambiguous→dispatch_ambiguous | **无 production-wiring 测试**（fake boundary 测试不证明 runPromptCommand 接线） | 唯一允许的 Stage 3 integration seam | 🔧 Stage 3 SHALL 把 `AgyPromptFreeProcess` 的写结果接回 Controller（与 ADM-R03 一致；旧版 ✅ 判定为假阳性已删除） |
| SIGTERM/SIGKILL 升级 | `cli.ts:1375-1419`（SIGINT→5s→SIGKILL）；`cli.ts:1421-1441` stopPty（kill→2s→SIGKILL）；`auth.ts:220-234`（SIGTERM→3s→SIGKILL） | `tests/auth.test.ts:95-155`；`tests/prompt-free-process.test.ts:298-314` | — | ✅（source 级 signal escalation 有测试） |
| descendant cleanup / startup permit 生命周期绑定（ADM-R03 相关） | `agy/startup-launcher.ts:149-204,206-265` | `tests/startup-launcher.test.ts`（19 用例） | `AgyStartupLifetimeBindingError` 保留 permit | ✅（source 级绑定有测试） |
| descendant cleanup 的 production receipt | startup-launcher/process-evidence 只有 fake/单元证据 | 未发现真实进程树级 integration receipt | 与 ADM 侧「SIGTERM/SIGKILL/descendant 的 production receipt 未真实取得」一致 | ❓ 尚未真实验证（Stage 3 test worker/runtime seam 提供） |
| stream-json identity-only 读取，不回传 provider payload（ADM-O02 相关） | `agy/stream-json-identity.ts:72-215`；`prompt-free-process.ts:43-44` | `tests/stream-json-identity.test.ts:28-60,62-98,117-125` | 只读 conversation_id；result status/error 被丢弃（→ 503 的 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN` carrier 面，见 §2.5） | ✅（primitive；其「丢弃 status/error」是 503 gap 的成因之一，已在 §2.5 记 disposition） |

## 3. 保留的正确事实（相对旧版不修改的结论）

以下事实经本轮重读 live source 复核后继续保留，与 Admission map 一致：

1. **disabled legacy 路径**：`runtime-config.ts:53-55` 默认关闭；`composeAcpRuntime`（agent.ts:706-718）disabled 分支原样返回；legacy JSON store 与 legacy prompt 路径不变；既有测试（cli/db/acp-server/queue-steer/auth）未因 admission 改写。
2. **session 单 turn 顺序**：`turn-scheduler.ts` I1-I4 不变量；`prompt.ts` busy→queue/steer；`tests/queue-steer.test.ts` 35 用例。
3. **现有在线输出路径**：`AgyCliSession.prompt` + StreamPoller + Translator + conversation SQLite + 客户端通知唯一路径；无第二 live 输出（§2.7）。
4. **结构化分类器与 cooldown ledger**：`errors.ts:36-56` 七类 + `controller.ts:726-737` cooldown；`tests/admission-errors.test.ts` 全矩阵（§2.5 第一行 ✅）。
5. **进程身份 primitive**：`process-evidence.ts` boot/PID/start-time/namespace/pgrp 全字段；`owner-instance.ts`；`startup-recovery.ts` 已证明退出路径（§2.6 ✅ 行）。
6. **no replay / no outer retry / forbidden absence**：`markActive`/`markExecutionRecoveryRequired`/`releaseExitedRecoverySeat` 删 payload；无 outbox/ACK/replay/requeue（§2.7 + §8）。

## 4. ACP v1 / v2 精确合同（terminal / API / data / event / permission）

### 4.1 方法面（RPC surface，与旧版一致，未修改）

- **v1**（`acp/agent.ts:726-744`）：`initialize`、`authenticate`、`logout`、`session/new`、`session/list`、`session/load`、`session/resume`、`session/setMode`、`session/setConfigOption`、`session/prompt`、`session/close`、`session/delete` + 通知 `session/cancel`。
- **v2**（`acp/agent.ts:754-768`）：`initialize`、`auth/login`、`auth/logout`、`session/new`、`session/list`、`session/resume`、`session/setConfigOption`、`session/prompt`、`session/close`、`session/delete` + 通知 `session/cancel`；v2 无 `session/setMode`、无 `session/load`；`initialize.protocolVersion` 协商（`agent.ts:770-780` `createDualAcpApp`）。
- **权限**：v1/v2 均经 `session/request_permission`（`acp/session/request-permission.ts`，所有权属于 ACP Connector）；`_meta["agy-acp/turnIntent"]="queue"|"steer"`（`prompt.ts:88-95`）；queued v2 取消 handle 经 `_meta["agy-acp/queuedPromptId"]`（prompt.ts:799-804, `cancel.ts:47-71`）。

### 4.2 Terminal 合同 — 现状与 F4 缺口（修正版）

| 状态 | v1 现状 | v2 现状 | Scheme 要求 | Disposition |
| --- | --- | --- | --- | --- |
| 正常完成 | `{stopReason:"end_turn"}` | `state_update{state:"idle", stopReason:"end_turn"}` | — | ✅ |
| 取消 | `{stopReason:"cancelled"}` | `state_update{state:"idle", stopReason:"cancelled"}` | 真实取消 terminal | ✅ |
| queue timeout | RPC error（`AdmissionQueueTimeoutError` 上抛，prompt.ts:709-712） | **`end_turn`**（prompt.ts:883） | 真实 typed terminal | 🔧 F4 |
| provider 失败（503 等） | RPC error | **`end_turn`**（prompt.ts:883） | 真实 typed terminal | 🔧 F4 |
| recovery_required | RPC error（`AdmissionTurnRecoveryRequiredError` 上抛） | **`end_turn`**（prompt.ts:883） | 真实 typed terminal | 🔧 F4 |
| queue 进度（观察） | `_paseo_queue_progress`（prompt.ts:411-420） | 同左 | 只读观察 | ✅ |

**F4 闭合（Stage 3 输入，单一物理合同）**：prompt-turn terminal 的唯一物理 owner 是 `ACP Connector/acp/session/prompt.ts`。v2 每 turn 恰好发一个 `state_update{state:"idle", stopReason:<code>}`；`stopReason` 保持开放字符串，本轮可输出 `end_turn|cancelled|queue_timeout|provider_failure|recovery_required`。v1 每 turn 恰好返回一个 `PromptResponse`：成功为 `{stopReason:"end_turn"}`；取消为 `{stopReason:"cancelled", _meta:{"agy-acp/turnTerminal":{version:1,code:"cancelled"}}}`；queue timeout/provider failure/recovery_required 均为 `stopReason:"refusal"`，并在同一 `_meta["agy-acp/turnTerminal"]` 中写 `{version:1,code:<对应分类>}`。已知 Scheme 失败不得 raw RPC throw、不得 `end_turn`，且必须与 `LiveTurnCompletion.failure` 分类一致（controller.ts:101-110）。`acp/terminal/index.ts` 只负责 execute-tool terminal display；`acp/session/update-wire.ts` 只转换 streamed `session/update` payload；两者都不是 prompt-turn terminal owner，不新增第二输出路径。**不声称 cancel 双终态存在**——当前 source 的单终态机制（lease fence + 幂等 release）未被任何证据证明会产生双终态，同时跨进程 race 全矩阵也未证明（ADM-R07 → 🧪）。

### 4.3 Data / Event 合同（与旧版一致）

- `controller.ts:59-72` 13 种 `SanitizedEventKind`；`:1492-1531` allowlist 过渡；`:1342-1365` 写前校验 + correlation HMAC；`readSanitizedEvents`（:777-786）identifier-free 分页。
- payload：AES-256-GCM + AAD（controller.ts:1367-1383, 1462-1464）；`expires_at` 到期即删（:458-481）；`content_fingerprint` 防换绑（:1454-1460, `PayloadConflictError` :338-343）。
- cooldowns：`(provider, model)` 复合主键，`not_before` MAX 合并（:726-737, :758-771）。

### 4.4 Permission 合同（新增显式 finding，PERMISSION_CHAIN_AMBIGUOUS 已判定）

| 事实 | source | 证据 |
| --- | --- | --- |
| admission enabled 时强制 `dangerously-skip-permissions` | `setup.ts:44-49`（buildSession：mode≠dsp 即抛；`promptInArgv=false`）；`agent.ts:480-488`（applyConfigOption：MODE_CONFIG_ID 且 value≠dsp 即抛 `AcpRuntimeCompositionError`） | 两处独立强制，非单一门 |
| admissionBoundary 拒绝 interactive permission 模式 | `cli.ts:535-539`（`promptInArgv || shouldUseInteractivePermissions()` → `AgyPromptFreeDispatchError("blocked","admission_requires_prompt_free_print_mode")`） | enabled 时 interactive 权限面板不可用 |
| `session/request_permission` 所有权 | `acp/session/request-permission.ts:20-77`（v1）、`:80-180`（v2）——请求/拒绝/elicitation 均由 ACP 客户端 RPC 驱动 | 所有权未转移给 Admission |
| 权限按键 PTY 写 vs 首次不可逆业务 prompt 写 | 前者：`cli.ts:1456-1485` `writePermissionKeys` → `pty.write(keys)`（interactive 面板导航键）；后者：`cli.ts:1138` `child.stdin.end(prompt)` / `prompt-free-process.ts:114-127` `writeBusinessPrompt`（业务 prompt + EOF） | **两类写语义不同**：权限键写不触发 admission 不可逆边界；业务 prompt 写必须经 dispatch boundary |
| Scheme 要求 | §4.2:98「启动后继续调用原有 … 权限与在线 ACP 通知链」；§7.2 closeout「legacy … 权限 … 路径保留」 | — |

**Disposition：🔧 偏离 Scheme，Stage 3 修复。** 当前 enabled 实现把权限链整体旁路（强制 dsp 模式），与 Scheme §4.2:98 的权限链保留义务冲突。Stage 3 SHALL 删除三处强制 skip 守卫，使 PTY 权限面板在 enabled 模式可达；`session/request_permission` 所有权继续归 ACP/client；权限按键 PTY 写不计入业务 dispatch；首次不可逆业务 prompt 写只经既有 `AgyAdmissionDispatchBoundary`（`TurnDispatchBoundary`）fence。不得新建第二权限/dispatch seam。**这不是「保持现状即可」的已确认行为，是已判定的偏离项。**

### 4.5 login/logout：enabled / disabled（F3，与旧版一致）

| 场景 | 现状 | Scheme 要求 | Disposition |
| --- | --- | --- | --- |
| v1 authenticate / v2 auth/login | `authenticate.ts:10-24` / `auth/login.ts:8-15`，无门禁 | enabled 时 fail closed | 🔧 F3 |
| v1 logout / v2 auth/logout | `logout.ts:9-27` / `auth/logout.ts:8-15`，无门禁 | enabled 时 fail closed | 🔧 F3 |
| disabled | 原行为保持（agent.ts:706-718） | 原行为保持 | ✅ |
| 测试 | `tests/acp-server.test.ts:123-190`（无 admission）；无 enabled×auth 测试 | 矩阵项 5 | 🔧（测试缺口并入 F3） |

## 5. cancel / steer / disconnect 竞态合同（修正版）

- **单 slot 所有权**：`turn-scheduler.ts:139-265`（active + steer reservations FIFO；`claimIdle` 同步；`promote` displace；`release` 幂等；`close` 全 abort）。
- **取消路径**：`cancel.ts:39-77`（queuedPromptId 精确命中；stale id 不 fallback 全局 abort）。
- **disconnect/session close**：`close.ts:11-26`、`delete.ts:21-37`（turns.close → cancelQueuedPrompts → agy.close）。
- **单终态/防重复机制**：lease fence（generation+ownerInstanceId）贯穿过渡（controller.ts:1299-1315, 1472-1489）；`AgyPromptFreeDispatchBoundary` 同步无 await 窗口（dispatch-boundary.ts:114-118, 166-173）；`settleFailure` 先查终态（turn-coordinator.ts:230-257）。
- **测试现状**：`tests/queue-steer.test.ts` 35 用例 + `tests/admission-fault-security-matrix.test.ts` 事务注入。
- **Disposition：🧪 测试不足或假阳性（ADM-R07）**——source 层单终态机制存在且正确，但跨进程 terminal/heartbeat/disconnect race 全矩阵未证明；不声称存在双终态缺陷（无证据），也不声称 race 全无（无跨进程测试）。

## 6. Provider 错误分类与可信 503 physical source（F11 专项，采纳 feasibility doc 锁定处置）

### 6.1 分类器与 ledger（保留 ✅）

`errors.ts:36-56` 七类结构化分类 + `controller.ts:726-737` cooldown ledger：**给定结构化输入时行为正确**，`tests/admission-errors.test.ts:75-108` 精确覆盖；`tests/admission-turn-coordinator.test.ts:261-296` 覆盖 cooldown 写入。这是回归证据，不是 provenance。

### 6.2 上游信号（SOURCE_FIELD_GAP）

`turn-coordinator.ts:366-385` `failureFromAgyError`：`error.message + error.stderr` 大写化正则猜 503/429/401/403 + 五字面量。`AgyCliError`（cli.ts:295-312）无结构化字段；stream-json `result` 的 status/error 被 identity reader 丢弃（stream-json-identity.ts:217-244）。

### 6.3 已授权 primary sources 内的证明结果（feasibility doc 结论，本文件采纳）

- 静态二进制 phrases（UNAVAILABLE/MODEL_CAPACITY_EXHAUSTED 等）存在，但**可对应多个错误类**（如 MODEL_CAPACITY_EXHAUSTED 与 RATE_LIMIT_EXCEEDED），且 strings(1) 不能证明 carrier/emission。
- 官方 headless 文档只有通用 `status/error` 与 stderr 规则，无 httpStatus/capacity code。
- 测试 `admission-turn-coordinator.test.ts:267-272` 的 stderr 是 fake oracle。
- **结论：`SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`（完整三元组，typed stop）。** 可信 capacity source 外部未解决；无 trusted-503 RED/实现就绪；不授权任何 phrase/regex 归一化；429/auth/permission/timeout/transport 分类保持独立（§2.5）；Stage 3 trusted-503 实现/RED 依赖门关闭，直到外部证据经单独授权形成可复核 receipt。本分支停止，不空转。

## 7. no outer retry / no replay（与旧版一致，保留）

| 不变量 | 证据 | 测试 |
| --- | --- | --- |
| 无外层业务 prompt 自动重试 | `turn-coordinator.ts` 无 retry loop；`dispatch-boundary.ts:129-182` 单次；`prompt-free-process.ts:110-127` once-only | `tests/admission-fault-security-matrix.test.ts:121-151`；`tests/agy-dispatch-boundary.test.ts:169-191`；`tests/prompt-free-process.test.ts:76-79,273-314` |
| 不确定 prompt 零自动重放 | `markActive`（controller.ts:601-610）、`markExecutionRecoveryRequired`（:644-670）、`releaseExitedRecoverySeat`（:677-706）均删 payload；无 requeue API | `tests/admission-turn-coordinator.test.ts:298-325`；`tests/admission-startup-recovery.test.ts:155-171` |
| 官方 CLI 内部重试仍属同一 turn/lease | `cli.ts:553-564` `shouldInstallAfterError` 仅 ENOENT 安装 | — |

## 8. Startup / runtime recovery（修正版）

- **启动期（✅，已证明）**：`admission/runtime.ts:68` → `recoverExitedAdmissionSeats`（startup-recovery.ts:46-106）→ `listRecoverableDispatches`（controller.ts:805-854）→ 按 connector/child/pgrp 证据 settle。`tests/admission-startup-recovery.test.ts:155-199` 覆盖 gone-tree/存活-child/inventory-unavailable 三路径。
- **运行期（🔧，缺失）**：无 reaper、无 heartbeat TTL/suspect、无 queued-owner 处置；`recovery_required` 计入 activeLeaseCount 且仅启动恢复可释放 → 无重启时三次不确定确实烧满 3 席（ADM-R04/R06 一致）。
- **pre-dispatch proof**：`process-evidence.ts:120-182,149-182` HMAC proof API 存在且有防伪测试（`tests/admission-process-evidence.test.ts:202-226`），但未接入 startup-recovery（ADM §11 `PROCESS_EVIDENCE_AMBIGUOUS`）；保留为 ❓ 未验证集成。

## 9. Legacy behavior preservation（与旧版一致）

Admission disabled 时：`composeAcpRuntime` 原样返回（agent.ts:706-718）；默认 JSON `SessionStore`；legacy prompt/权限/取消/history 路径不变；既有测试未改写（git log 8e03bea/463392a/4d3cd46 为 admission 独立提交）。→ ✅

## 10. 禁止恢复项（与旧版一致，absence 正确）

outbox / delivery claim / custom ACK / terminal reconnect replay / client route fencing / 第二 live SQLite→ACP / shadow/live parity / custom request identity / manual requeue / recovery claim / exact-conversation binder / startup-permit / 大型 migration 框架 / MAACS ledger/supervisor/runtime sync/Stage 2 门禁——当前 HEAD 均不存在（schema.ts:281-291 意外表 fail closed；CHANGELOG.md:36-46 Removed；`isExactFreshPtyAgyLaunch` 恒 false（launch-spec.ts:164-177））。→ ⛔ 全部禁止恢复

## 11. Documentation RED 处置（8 项，修正后）

| Finding | Severity | 处置 |
| --- | --- | --- |
| F3 login/logout fail-closed missing | HIGH/blocking | ✅闭合：§4.5 门禁设计 + v1/v2 四方法测试矩阵项 5 |
| F4 typed terminal missing | HIGH/blocking | ✅闭合：§4.2 单一 typed-terminal wire 合同与唯一 `prompt.ts` owner；v2 failure→end_turn 为 source-proven 偏离；v1 typed failure contract 缺失 |
| F5 PASEO_LOCAL_CHANGES 过期 | MEDIUM | 文档集成输入：重建为当前 HEAD 清单或重定位 + admission 区段 |
| F9 README scope 矛盾 | MEDIUM | 文档集成输入：改写 exact scope 或删句统一分层事实表 |
| F11 可信 503 source gap | HIGH/blocking | ✅闭合为完整三元组 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`（§6）；分支停止；依赖门关闭 |
| F14 RED/GREEN 缺失 | HIGH/blocking | 方法层闭合：本文件每条 disposition 绑定 deterministic test file:line 作为 Stage 3 RED receipt 初始输入 |
| F16 design doc「Paseo 0.4.0」无依据 | LOW | 文档集成输入：删除或绑定来源，标注未验证 |
| F17 fail-closed 枚举弱化 | LOW | 文档集成输入：三份文档统一为 Scheme 完整枚举（§2.1 行） |

（F1/F2/F6/F7/F8/F10/F12/F15/F18 不属于本 worker 写集；F1/F2/F7/F12 的 seam 侧证据已在 §2 记录，与 Admission map 一致。）

## 12. 未验证事实与 typed blockers

| # | 事实 | 状态 | 处置 |
| --- | --- | --- | --- |
| 1 | 可信 503 physical source（§6.3） | **SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN**（完整三元组） | 分支停止；Stage 3 依赖门关闭（feasibility doc §6 两条件），不臆造 |
| 2 | admission enabled 强制 dsp 的权限链旁路（§4.4） | **PERMISSION_CHAIN_AMBIGUOUS → 已判定为偏离** | 🔧 Stage 3 修复（不再作为未决歧义） |
| 3 | 跨进程 terminal/heartbeat/disconnect race 全矩阵（§5） | **PROCESS_EVIDENCE_AMBIGUOUS（测试缺口，机制存在）** | 🧪 Stage 3 矩阵项 8 新增跨进程用例；不声称双终态 |
| 4 | 三次 recovery_required 无重启释放（§2.6） | **PROCESS_EVIDENCE_AMBIGUOUS（运行期路径缺失）** | 🔧 Stage 3 运行期 bounded 释放/reaper |
| 5 | v1/v2 typed terminal 精确 wire 形状 | **已锁定，非 blocker** | §4.2 固定 v2 单 idle `state_update` 与 v1 单 `PromptResponse` + typed meta；Stage 3 只按该合同实现并测试 |
| 6 | pre-dispatch proof 未接入 startup recovery | ❓ | Stage 3 裁决权威 proof（ADM §11 一致） |
| 7 | 真实 Provider 行为（真实 503/stream-json 错误/login 交互） | ❓ 本 Stage 禁止运行（Scheme §8:293） | 全部归 Stage 3 隔离观测，不臆造 |
| 8 | AGENTS.md:4 GitNexus 统计过期（3129/9279/273 vs live 2819/8424/245） | 过期（F6） | 文档集成更新或删除数字 |

**汇总**：`SOURCE_FIELD_GAP`（#1，已停止）、`REAL_PROVIDER_REQUIRED`（#1，typed stop）、`SOURCE_IDENTITY_UNKNOWN`（#1，typed stop）、`PERMISSION_CHAIN_AMBIGUOUS`（#2，已判定为偏离而非静默）、`PROCESS_EVIDENCE_AMBIGUOUS`（#3/#4，机制与缺口分离）。typed-terminal wire shape 已由 §4.2 锁定，不再是 blocker。无 `SCHEME_HASH_MISMATCH`、无 `AUTHORITY_CONFLICT`、无 `DIRTY_OWNERSHIP_UNKNOWN`、无 `WRITE_SET_VIOLATION`。

## 13. Stage 2 boundary 与 closeout（含 REPAIR-002 至 REPAIR-006 修复记录）

- 唯一写入：`docs/design/v2.0.0.0-stage2-acp-source-map.md`；未改 source/test/package/README/CHANGELOG/其他 artifact/MAACS/`~/.paseo`/`~/.agents`/git index/refs；未 build/test/provider/6767/network/commit；未委派。
- **REPAIR-002 修复记录**：删除本文件中所有 sibling artifact 的 whole-file digest 声明（503 feasibility 与 Admission source map 的 SHA-256 均移除，改为「path + task identity + 语义角色 + 外部绑定说明」）；不以内嵌或更新的 digest 替代；不携带本文件自身 digest；trusted-503 所有 disposition 统一为完整三元组 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`；source-symbol/test/evidence、F7 `agent_id/agentId`、permission-chain、production-dispatch 等结论行未改动。
- **REPAIR-003 修复记录（本次）**：503 兄弟 artifact 依赖引用统一为根 task identity `S2-SPIKE-503-001`（feasibility doc 路径 `docs/design/v2.0.0.0-stage2-503-feasibility.md`）；不引用任何 sibling repair suffix；删除全部被拒选项/替代方案叙述，仅保留 canonical 陈述：可信 capacity source 外部未解决，完整三元组 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN` typed stop，无 trusted-503 RED/实现就绪，不授权任何 phrase/regex 归一化；sibling-digest 移除与非 503 evidence 全部保留。
- **REPAIR-004 修复记录**：以 `acp/session/prompt.ts` 为 prompt-turn terminal 唯一物理 owner；§4.2 固定 v2 单 idle `state_update` 与 v1 单 `PromptResponse` + `_meta["agy-acp/turnTerminal"]` 的 wire shape；辅助 display/converter 保持窄职责；权限链改用 architecture 已锁定的既有 fencing seam，不保留实现选项。
- **REPAIR-005 修复记录**：以 `scheme-bound-artifact-repair` + `scheme-artifact-authoring` + `canonical-artifact-hash-and-scan-discipline` 执行 bounded reconciliation；所有 trusted-503 语义均使用 `SOURCE_FIELD_GAP + REAL_PROVIDER_REQUIRED + SOURCE_IDENTITY_UNKNOWN`，source-gap 义务行保持单一 typed-stop disposition，classifier/cooldown 正确性仍只作独立 regression evidence。本轮未改 source/test/index/refs，whole-file SHA 仅在最终扫描后从文件外计算。
- **REPAIR-006 修复记录**：采纳 fresh Formal Spec Review LOW-1，按 live `cli.ts:1132-1142` 将 production dispatch 描述改为 inline hooks 已存在、prompt-free 三态结果映射缺失；ADM-R03 与 T12 合同不变。未改 source/test/index/refs，whole-file SHA 仅在最终扫描后从文件外计算。
- 与 Admission source map 对齐行：ADM-R01（heartbeat 🔧）、ADM-R03（production wiring 🔧，primitive ✅）、ADM-R04（全 phase recovery 🔧）、ADM-R06（queued owner 🔧）、ADM-O01（typed terminal 🔧）、ADM-D03（idle resident ❓）——无 contradiction（对齐内容以路径 + 行号引用，不引用 digest）。
- F7 canonical 目标：`agent_id/agentId`；`parent_id/parentId` 仅为 migration input。
- 每行唯一 disposition；旧版混合行已拆分。
- 本文件不内嵌自身 hash；whole-file SHA-256 由外部计算，在最终报告中给出。
